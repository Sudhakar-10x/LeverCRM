<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">

            <li class="nav-header">
                <div class="profile-element text-center">
                    <img src="~/Content/RoundLogoAL.png" alt="Logo" class="img-circle" style="width:60px; height:60px;" />
                </div>
            </li>

            <li class="@(Html.IsSelected(controller: "Home") == "active" ? "active" : "")">
                <a href=@Url.Action("Index","Home") class="fa fa-dashboard">
                    <i class="fa fa-tachometer-alt"></i>
                    <span class="nav-label">Dashboard</span>
                </a>
            </li>


            @*<li class="@(Html.IsSelected(controller: "Product") == "active" || Html.IsSelected(controller: "BusinessPartner") == "active" ? "active" : "")">
                <a href="#"><i class="fa fa-database"></i> <span class="nav-label">Masters</span> <span class="fa arrow"></span></a>
                <ul class="nav nav-second-level">
                    <li class="@Html.IsSelected(controller: "Product")">
                        <a href="@Url.Action("Index", "Product")">Product Details</a>
                    </li>
                    <li class="@Html.IsSelected(controller: "BusinessPartner")">
                        <a href="@Url.Action("Index", "BusinessPartner")">Business Master List</a>
                    </li>
                </ul>
            </li>

            <li class="@(Html.IsSelected(controller: "SalesQuotation") == "active" ? "active" : "")">
                <a href="#"><i class="fa fa-shopping-cart"></i> <span class="nav-label">Sales</span> <span class="fa arrow"></span></a>
                <ul class="nav nav-second-level">
                    <li class="@Html.IsSelected(controller: "SalesQuotation")">
                        <a href="@Url.Action("Index", "SalesQuotation")">Sales Quotation</a>
                    </li>
                </ul>
            </li>

            <li class="@(Html.IsSelected(controller: "Report") == "active" ? "active" : "")">
                <a href="#"><i class="fa fa-shopping-bag"></i> <span class="nav-label">Reports</span> <span class="fa arrow"></span></a>
                <ul class="nav nav-second-level">
                    <li class="@Html.IsSelected(controller: "Report")">
                        <a href="@Url.Action("Index", "Report")">Item Availability</a>
                    </li>
                </ul>
            </li>*@

        </ul>
    </div>
</nav>

<!-- CSS for Active State -->
<style>
    /* 🔴 Main menu active highlight */
    .nav > li.active > a {
        background-color: #e91e63 !important; /* red highlight */
        color: #fff !important;
        font-weight: bold;
    }

    /* Submenu default */
    .nav-second-level > li > a {
        position: relative;
        padding-left: 25px;
        color: #bbb !important; /* light gray default */
        display: block;
        transition: all 0.3s ease;
    }

        /* Dot for submenu */
        .nav-second-level > li > a::before {
            content: "•";
            color: #999;
            position: absolute;
            left: 10px;
            font-size: 14px;
            line-height: 20px;
        }

    /* ✅ Active submenu (NO background, just text highlight) */
    .nav-second-level > li.active > a {
        font-weight: bold;
        color: #e91e63 !important; /* red text for active submenu */
        background: none !important; /* ❌ remove red background */
    }

        /* 🔴 Active submenu dot */
        .nav-second-level > li.active > a::before {
            color: #e91e63; /* red dot */
        }

    /* Hover effect */
    .nav-second-level > li > a:hover {
        color: #fff !important;
        background: none !important; /* ❌ no background */
        text-decoration: underline; /* subtle hover effect */
    }
</style>



@*<nav class="navbar-default navbar-static-side" role="navigation">
        <div class="sidebar-collapse">
            <ul class="nav" id="side-menu">

                <li class="nav-header">
                    <div class="profile-element text-center">
                        <img src="~/Content/10X_Round.png" alt="Logo" class="img-circle" style="width:60px; height:60px;" />
                    </div>
                </li>


                <li>
                    <a href="#"><i class="fa fa-database"></i> <span class="nav-label">Masters</span> <span class="fa arrow"></span></a>
                    <ul class="nav nav-second-level">
                        <li class="@Html.IsSelected(controller: "Product")">
                            <a href="@Url.Action("Index", "Product")">Product Details</a>
                        </li>
                        <li class="@Html.IsSelected(controller: "BusinessPartner")">
                            <a href="@Url.Action("Index", "BusinessPartner")">Business Master List</a>
                        </li>
                    </ul>
                </li>

                <li>
                    <a href="#"><i class="fa fa-shopping-cart"></i> <span class="nav-label">Sales</span> <span class="fa arrow"></span></a>
                    <ul class="nav nav-second-level">
                        <li class="@Html.IsSelected(controller: "SalesQuotation")">
                            <a href="@Url.Action("Index", "SalesQuotation")">Sales Quotation</a>
                        </li>

                    </ul>
                </li>

            </ul>
        </div>
    </nav>*@